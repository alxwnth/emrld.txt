<!--TODO: IDK why doesn't it work as normal, will investigate later-->
<%# @task = Task.find(params[:id]) %>
<div>
  <turbo-frame id="<%= dom_id @task %>">
    <%= render "form", task: @task %>
    <%= link_to "Never Mind", tasks_path, class: "btn mb-3 bg-gray-100" %>
  </turbo-frame>
</div>
